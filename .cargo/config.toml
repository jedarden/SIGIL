[env]
PYO3_USE_ABI3_FORWARD_COMPATIBILITY = "1"

# Note: Local Nix-based development uses a custom linker in .cargo/config.local.toml
# CI containers use the default gcc from Debian, so no custom linker needed here.
