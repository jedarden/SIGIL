# Rust build artifacts
/target/
**/target/
Cargo.lock

# Node.js build artifacts
node_modules/
*.node
package-lock.json

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Marathon session files (Claude Code)
.marathon/

# Compiled binaries (already covered by /target/ and **/target/)
# These are listed here for clarity but are redundant
sigil
sigild
sigil-shell
sigil-tui
sigil-mcp

# Test coverage
*.profraw
*.profdata

# Temporary files
*.tmp
*.bak
*.backup

# Environment files
.env
.env.local
.env.*.local

# Logs
*.log
logs/

# Database files
*.db
*.db-shm
*.db-wal
.beads/

# OS files
Thumbs.db
.DS_Store

# Build output
/dist/
/build/
out/

# SIGIL vault directories (test vaults, local vaults)
.sigil/
!.beads/issues.jsonl

# Local cargo configuration (system-specific, e.g., Nix linker paths)
.cargo/config.local.toml

# Fuzzing: ignore generated corpus and artifacts, keep seed corpus
fuzz/corpus/
fuzz/artifacts/
fuzz/target/
!fuzz/corpus_seed/

# Needle session files (AI coding tool)
.needle.yaml
.needle-predispatch-sha
fuzz_run_*.log

# Test vault directories (runtime test artifacts)
test-vault*/
test-vault-cli*/
test-cli-verify*/

# Target backup directories (generated by cargo/rustc)
target.bak*/
target.old.*/

# mdBook build output (build locally with `mdbook build` in docs/)
docs/book/
